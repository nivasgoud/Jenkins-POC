#!/bin/bash

# Example deployment steps
echo "Deploying application on $(hostname)"
# Add your deployment steps here, for example:
# sudo systemctl restart myservice
# or
# docker pull myapp:latest && docker run -d myapp:latest